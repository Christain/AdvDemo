<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.iclicash.advdemo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="union_app_id"
            android:value="52oMdyprg5AYS3Qy" />

        <meta-data
            android:name="aiclk_override_adbrowser_tvtitle_text_color"
            android:value="9139f5" />

        <meta-data
            android:name="aiclk_override_adbrowser_tvback_text_color"
            android:value="9139f5" />

        <meta-data
            android:name="aiclk_override_adbrowser_tvclose_text_color"
            android:value="9139f5" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.iclicash.advlib.ui.front.ADBrowser"/>
    </application>

</manifest>